import React from "react";
import styled from "styled-components";

const Section = styled.section``;

const Home = () => {
  return (
    <Section>
      <div>Home Page</div>
    </Section>
  );
};

export default Home;
